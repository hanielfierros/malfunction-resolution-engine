services:
  - type: web
    name: malfunction-resolution-engine
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: ENVIRONMENT
        value: production
      - key: ALLOWED_ORIGINS
        sync: false
